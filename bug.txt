 Configuring project ':feature:feature-player' without an existing directory is not allowed. The configured projectDirectory 'D:\dev\mobile\radio\feature\feature-player' does not exist, can't be written to or is not a directory.


* Try:

> Make sure the project directory exists and is writable.

> Run with --scan to generate a Build Scan (Powered by Develocity).

Fix with AI


AILURE: Build failed with an exception.


* What went wrong:

Configuring project ':feature:feature-player' without an existing directory is not allowed. The configured projectDirectory 'D:\dev\mobile\radio\feature\feature-player' does not exist, can't be written to or is not a directory.


* Try:

> Make sure the project directory exists and is writable.

> Run with --scan to generate a Build Scan (Powered by Develocity).


BUILD FAILED in 37s


Download https://services.gradle.org/distributions/gradle-9.0.0-bin.zip, took 42 s 33 ms

It is not possible to use the currently selected Gradle JVM for the operation.

The installation from C:\Program Files\Amazon Corretto\jdk21.0.10_7 will be used instead.

Starting Gradle Daemon...

Gradle Daemon started in 2 s 572 ms

Configuration on demand is an incubating feature.


[Incubating] Problems report is available at: file:///D:/dev/mobile/radio/build/reports/problems/problems-report.html




Fehler beim Gradle Sync: Configuring project ':feature:feature-player' without an existing directory is not allowed.

Ursache: Du hast die Ordnerstruktur beim Generieren komplett zerstört. Anstatt echte Unterordner (wie feature/feature-player/) auf dem Dateisystem anzulegen, hast du flache Dateien erzeugt, die den kompletten Windows-Pfad als Dateinamen tragen (mit ∷ als Trennzeichen).

Aufgabe: Bitte bereinige das. Lösche alle diese fehlerhaft benannten Dateien. Generiere danach die korrekte, physisch verschachtelte Ordnerstruktur und lege die Dateien in den echten Unterordnern ab!